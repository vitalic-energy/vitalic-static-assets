# cooldown satisfies semgrep p/default dependabot-missing-cooldown (blocking in kb repos)
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
