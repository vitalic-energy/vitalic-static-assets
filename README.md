# vitalic-static-assets

Public static assets for Vitalic Energy web properties — brand SVGs that other
Vitalic sites embed by URL. There is no application, build or deploy: the files
in the repository root are the product. CI checks that every SVG is well-formed,
size-capped and free of scripts or remote references.
